# Social feedback inbox

Telegram change-requests captured by `agents/social-feedback.ts`. Each
entry = a timestamp + the delivered post slug + Abdullah's exact words.
Claude applies these in-session (re-render, re-gate, resend), then clears
the handled entries. Approvals and skips are auto-handled and not logged
here.

<!-- entries appended below -->
